# Old Inverse-Problem Scaffold

This folder contains earlier scaffold files from a previous version of the repository.

These files are preserved for historical reference only. They are not part of the current main workflow.

The current project focuses on VAE reconstruction and downstream X-ray classification under sample scarcity.

Use the scripts in the main `scripts/` folder for the current reproducible workflows.